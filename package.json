{
  "name": "token-sync-monorepo",
  "private": true,
  "workspaces": [
    "packages/lib",
    "packages/figma-plugin",
    "packages/demo"
  ],
  "scripts": {
    "build": "npm run build:lib && npm run build:plugin",
    "build:lib": "npm run build -w packages/lib",
    "build:plugin": "npm run build -w packages/figma-plugin",
    "dev:demo": "npm run dev -w packages/demo"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
