{
  "name": "token-beam-aseprite-plugin",
  "displayName": "⊷ Token Beam",
  "description": "Sync design tokens (colors) to Aseprite palette",
  "version": "0.1.0",
  "author": { "name": "Token Beam" },
  "contributors": [],
  "publisher": "token-beam",
  "license": "AGPL-3.0-or-later",
  "categories": [ "Scripts" ],
  "contributes": {
    "scripts": [
      { "path": "./token-beam.lua" }
    ]
  },
  "scripts": {
    "install:aseprite": "mkdir -p \"$HOME/Library/Application Support/Aseprite/scripts/token-beam\" && SYNC_URL=\"${SYNC_SERVER_URL:-ws://localhost:8080}\" && sed \"s|^local syncServerUrl = \\\".*\\\"|local syncServerUrl = \\\"$SYNC_URL\\\"|\" token-beam.lua > \"$HOME/Library/Application Support/Aseprite/scripts/token-beam/token-beam.lua\" && cp -f package.json \"$HOME/Library/Application Support/Aseprite/scripts/token-beam/\"",
    "uninstall:aseprite": "rm -rf \"$HOME/Library/Application Support/Aseprite/scripts/token-beam\""
  }
}
